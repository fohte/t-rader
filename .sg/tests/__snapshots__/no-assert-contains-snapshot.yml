id: no-assert-contains
snapshots:
  ? |
    assert!(
        result.contains("expected"),
        "should contain expected"
    );
  : labels:
      - source: |-
          assert!(
              result.contains("expected"),
              "should contain expected"
          )
        style: primary
        start: 0
        end: 73
  assert!(names.contains(&"feature-a"));:
    labels:
      - source: assert!(names.contains(&"feature-a"))
        style: primary
        start: 0
        end: 37
  assert!(result.contains("test"));:
    labels:
      - source: assert!(result.contains("test"))
        style: primary
        start: 0
        end: 32
