[generation]
# PostgreSQL の TEXT/VARCHAR カラムは null バイトを受け付けないため、
# ミドルウェアでグローバルに拒否している。OpenAPI の string 型は
# 通常 null バイトを含まない前提で使われるため、生成自体を無効化する。
allow-x00 = false

[checks]
# GET/DELETE エンドポイントのパスパラメータは URL 文字列にシリアライズされるため、
# Schemathesis が型をミューテートしても有効な文字列として送信される。
# stateful テストで作成されたリソースの UUID が再利用されると、
# サーバーが正常応答を返し false positive になる。
# ref: https://github.com/schemathesis/schemathesis/issues/2312
negative_data_rejection.enabled = false

# パスパラメータに含まれる UUID はランダム生成されるため、
# 対応するリソースが存在せず 404 が返る。これはスキーマ準拠のデータが
# 拒否されたのではなく、リソースが存在しないだけなので false positive になる。
# stateful テストでは実際のリソース ID が使われるため正しく検証される。
positive_data_acceptance.enabled = false

# chat エンドポイントは agent task client 未設定時に 503 を返す仕様
# (CI 環境では TRADER_AGENT_API_URL=disabled のため必然的に 503 になる)。
# ref: https://github.com/schemathesis/schemathesis/issues/2539
[[operations]]
include-path-regex = "^/api/strategies/[^/]+/chat$"
[operations.checks.not_a_server_error]
expected-statuses = ["2xx", "4xx", 503]

# preview エンドポイントは Kata exec runtime 未設定時に 503 を返す仕様
# (CI 環境では KATA_EXEC_API_URL を設定しないため必然的に 503 になる)。
[[operations]]
include-path-regex = "^/api/indicators/preview$"
[operations.checks.not_a_server_error]
expected-statuses = ["2xx", "4xx", 503]

# hook 受信エンドポイントは agent task client 未設定時に 503 を返す仕様
# (CI 環境では TRADER_AGENT_API_URL=disabled のため必然的に 503 になる)。
# ref: https://github.com/schemathesis/schemathesis/issues/2539
[[operations]]
include-path-regex = "^/api/hooks/[^/]+$"
[operations.checks.not_a_server_error]
expected-statuses = ["2xx", "4xx", 503]

# note/annotation の reject エンドポイントは却下時に agent task を投入するため、
# agent task client 未設定時に 503 を返す仕様
# (CI 環境では TRADER_AGENT_API_URL=disabled のため必然的に 503 になる)。
# ref: https://github.com/schemathesis/schemathesis/issues/2539
#
# TODO: このブロックは該当 endpoint の 503 を無条件に許容するため、実際の 5xx バグも
# 隠してしまう。また reject/chat/hooks は 503 で早期 return するため fuzzing が
# その先のロジックに到達せず coverage が無い。本来は CI で agent internal API のスタブを
# 立てて TRADER_AGENT_API_URL を実 URL にし、この allowlist を撤去すべき。
# スタブは backend の HttpAgentTaskClient (agent_client/client.rs) と agent の
# internal API routes (agent/src/internal-api/routes.ts) の契約に追従し続ける必要があり、
# 両者は現状共有スキーマを持たない手写しミラー。契約の期待値を 1 つの fixture ファイルに
# 寄せ、agent のテスト・backend の client テスト・CI のスタブの 3 箇所が同じファイルを
# 参照するようにすれば、この allowlist を撤去できる。
[[operations]]
include-path-regex = "^/api/(notes|annotations)/[^/]+/reject$"
[operations.checks.not_a_server_error]
expected-statuses = ["2xx", "4xx", 503]

[phases.fuzzing]
# Fuzzing フェーズはランダム生成された UUID パスパラメータにより
# 存在しないリソースへのリクエストが大量に発生し、4xx レスポンスが
# schema validation mismatch として検出される。Coverage フェーズで
# 同じ failure が既に報告された場合、重複排除により "No issues found" と
# 表示されるが PhaseFinished(status=FAILURE) が exit code 1 を返す。
# Coverage フェーズと Stateful フェーズで十分にカバーされるため無効化する。
enabled = false
