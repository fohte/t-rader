# MCP server

t-rader-backend (Axum) 内に 2 つの MCP server (`rmcp` ベースの Streamable HTTP) を同居させ、利用者ごとに別 path で露出する。

| path            | 用途                                                                                                  | 主な利用者                                                                             |
| --------------- | ----------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| `/mcp/mgmt`     | 管理 MCP。戦略状態の参照と戦略タスク投入                                                              | 外部のコントロールプレーンクライアント、t-rader-agent (戦略解決時の `list_strategies`) |
| `/mcp/strategy` | 戦略実行 MCP。戦略 Agent が戦略境界内のリソース (ノート / アノテーション / 価格 / Python 実行) を操作 | t-rader-agent                                                                          |

両 path とも JSON-RPC over Streamable HTTP (SSE) で通信する。`rmcp` は MCP spec [2026-07-28](https://modelcontextprotocol.io/specification/2026-07-28) までのプロトコル世代を自動判別して同時に扱う。

- **`< 2026-07-28` (レガシー)**: `initialize` → `mcp-session-id` ヘッダで以後のリクエストを継続する。t-rader-agent (`@modelcontextprotocol/sdk`) はこの世代で接続する。
- **`2026-07-28` 以降**: SEP-2567 により session の概念が無くなり、リクエストごとに stateless に処理される (`mcp-session-id` は発行されない)。discover lifecycle (SEP-2575) を使う外部クライアントはこの世代で接続する。`initialize` を経ず `MCP-Protocol-Version` ヘッダのみで直接 tool を呼ぶ場合、SEP-2243 の `Mcp-Method` ヘッダも必須になる (`Mcp-Name` は `tools/call` など対象を名指しする method でのみ必須)。

## 管理 MCP (`/mcp/mgmt`)

外部のコントロールプレーンクライアントから呼ばれる。tool 単位の認可は持たず、ネットワーク境界 (VPN / Zero Trust proxy 等) と前段認証で担保する想定。

| tool                       | 入力                                                                                                              | 出力 (要約)                                                                                                  |
| -------------------------- | ----------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `list_strategies`          | (なし)                                                                                                            | 戦略一覧 (`strategy_id`, `name`, `updated_at`, `unread_card_count`)                                          |
| `submit_strategy_task`     | `strategy_id`, `prompt`, `purpose?`                                                                               | `task_id`, `a2a_task_id`。DB に `strategy_task` 行を作り t-rader-agent にタスクを投入する                    |
| `get_strategy_task_status` | `a2a_task_id`                                                                                                     | `phase` (`pending` / `running` / `completed` / `failed`), `error_summary`, `result_text`, `updated_at` 等    |
| `get_strategy_config`      | `strategy_id`                                                                                                     | 戦略設定一式 (`name`, `description`) と紐づく `triggers` 一覧                                                |
| `create_strategy`          | `name`, `description?`                                                                                            | `ok`, `errors`, `strategy_id?`。検証エラーがあれば 1 件も書き込まず `ok=false` で全エラーを返す              |
| `update_strategy_config`   | `strategy_id`, `name?`, `description?`                                                                            | `ok`, `errors`。指定フィールドのみ 1 回の呼び出しで atomic に更新する                                        |
| `delete_strategy`          | `strategy_id`, `confirm_name`                                                                                     | `ok`, `errors`。`confirm_name` が戦略名と完全一致しないと削除しない。一致すれば関連リソースごと cascade 削除 |
| `create_strategy_trigger`  | `strategy_id`, `kind` (`cron` / `hook`), `schedule?`, `hook_slug?`, `event_match?`, `prompt_template`, `enabled?` | `ok`, `errors`, `trigger_id?`。kind=cron は schedule 必須 (hook_slug 禁止)、kind=hook はその逆               |
| `update_strategy_trigger`  | `trigger_id`, `schedule?`, `hook_slug?`, `event_match?`, `prompt_template?`, `enabled?`                           | `ok`, `errors`。指定フィールドのみ更新。kind / strategy_id は不変                                            |
| `delete_strategy_trigger`  | `trigger_id`                                                                                                      | `ok`, `errors`                                                                                               |
| `list_recent_notes`        | `strategy_id`, `limit?`                                                                                           | 最新ノートのメタデータ一覧                                                                                   |
| `list_recent_annotations`  | `strategy_id`, `limit?`                                                                                           | 最新アノテーションのメタデータ一覧                                                                           |
| `list_rss_feeds`           | `enabled_only?`                                                                                                   | RSS フィード定義一覧                                                                                         |
| `create_rss_feed`          | `source`, `display_name`, `url`, `enabled?`                                                                       | 作成した RSS フィード定義                                                                                    |
| `update_rss_feed`          | `id`, `display_name?`, `url?`, `enabled?`                                                                         | 更新後の RSS フィード定義                                                                                    |
| `delete_rss_feed`          | `id`                                                                                                              | `id`。既存の `news_item` 行は残す                                                                            |

`submit_strategy_task` は t-rader-agent の内部 API (`POST /internal/tasks`) 経由でタスクを投入する。クライアント実装は `backend/src/agent_client/` が SSOT。投入から決着までの共通ロジックは `backend/src/services/strategy_tasks.rs`、決着 polling は `backend/src/mcp/watcher.rs` を参照。

`create_strategy` / `update_strategy_config` / `delete_strategy` による DB 書き込みは REST (`backend/src/handlers/strategies/mod.rs`) と共通の `backend/src/services/strategy_config.rs` を経由し、`change_history` には actor `llm` / label `mgmt-mcp` で記録される。

`create_strategy_trigger` / `update_strategy_trigger` / `delete_strategy_trigger` は REST (`POST /api/strategies/{id}/triggers`, `PUT /api/triggers/{trigger_id}`, `DELETE /api/triggers/{trigger_id}`) と共通の `backend/src/services/trigger_crud.rs` を経由する。`change_history.target_kind` の CHECK 制約は `"trigger"` を含まないため、trigger への書き込みは change_history に記録されない (既知の監査ギャップ)。

## 戦略実行 MCP (`/mcp/strategy`)

t-rader-agent からのみ呼ばれる想定。接続元の絞り込みはクラスタ側のネットワーク境界で組み立てる。

### 戦略境界の保証

t-rader-agent は接続時に `x-strategy-id` HTTP ヘッダで自身が実行中の `strategy_id` を持ち込む。

- ヘッダが欠落 / 非 UUID なら MCP 層で reject する。
- tool 引数の `strategy_id` がヘッダの値と一致しない呼び出しも reject する。
- 対象リソース (note / annotation) の `strategy_id` も Repository 層で二重検査し、戦略 A の Agent が戦略 B のリソースに触れないことを保証する。
- 例外がある。`read_portfolio` は、戦略は口座内のお金の区分に過ぎず分析は口座全体を見る、という設計上、口座全体の集計には `x-strategy-id` をスコープとして使わない (ただし接続元戦略自身のスライス (投資可能額を含む) を追加で返すため、ヘッダの値もその選択に使う)。`search_refs` / `add_ref_terms` / `remove_ref_terms` は stock/indicator/sector/theme が戦略に属さないマスタデータであるため、`x-strategy-id` をそもそも検索・更新条件に使わない。`search_news` も news_item 全体を検索対象にするため、`x-strategy-id` を検索条件に使わない。`read_shareholding_structure` も EDINET 保有構造データが戦略に属さない市場データであるため、`x-strategy-id` を検索条件に使わない。`read_fin_summary` も財務情報が会社単位の開示であり戦略に属さないマスタデータであるため、`x-strategy-id` を検索条件に使わない。`read_macro_indicator` もマクロ指標の観測値が戦略に属さない市場データであるため、`x-strategy-id` を検索条件に使わない。`read_trades` も `read_portfolio` の account スコープと同じ規則で全戦略横断の trade を対象にし、`x-strategy-id` を検索条件に使わない (返り値の `strategy_id` でどの戦略の約定かを判別する)。`read_margin` も信用残が銘柄単位の市場データであり戦略に属さないため、`x-strategy-id` を検索条件に使わない。`read_short_sale_reports` / `read_sector_short_ratio` も空売り関連データが銘柄や業種単位の市場データであり戦略に属さないため、`x-strategy-id` を検索条件に使わない。

任意ヘッダ `x-execution-id` で実行単位を識別できる。値の単位は t-rader-agent 側の実行経路で異なる: agent_graph 実行ではフェーズ / for_each 要素ごとの実行ステップ 1 件 (`{a2a_task_id}:{step_id}`)、agent_graph 未設定の単一 invoke では `runStrategyAgent` 呼び出し全体 (A2A タスクの `taskId` そのもの) が単位になる。戦略境界の検査対象ではなく、`write_note` / `create_annotation` の resume 時重複排除や `read_news` の checkpoint 記録 (下表参照) に使う。resume のたびに同じステップへ新しい `a2a_task_id` が発行されるため、`write_note` の冪等性キーは `step_id` 部分のみを使い (`a2a_task_id` が変わっても同じノートに収束させる)、`create_annotation` は `step_id` と `a2a_task_id` を分けて保持し resume 時の置換要否の判定に使う。

### tool 一覧

| tool                          | 入力                                                                                                                                                  | 出力 (要約)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| ----------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `query_data`                  | `strategy_id`, `instrument_ids` (配列, 最大 100 件, 重複不可), `from` (YYYY-MM-DD), `to` (YYYY-MM-DD)                                                 | 各銘柄の日足バー時系列 (DB から取得。`instrument_ids` と同じ順序で返し、データが無い銘柄は空配列)。外部データプロバイダへのフォールバックはしない                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| `write_note`                  | `strategy_id`, `note_id?`, `title?`, `body_md?`, `type_tag?`, `frontmatter_json?`, `graphs?`                                                          | ノートを作成または更新。`created_by_kind` はサーバー側で `"llm"` 固定。更新時は status を unread に戻す。`graphs` (`GraphDef` の配列、定義は `backend/src/services/graph.rs`) は配列ごと置換 (id 単位の部分更新は不可)。省略時は既存の図を変更しない。`note_id` 省略時、`x-execution-id` ヘッダがあれば同一 `step_id` の既存ノートを探して更新し、複数の create 呼び出しが同一ノートへ収束する (idempotent。resume で `a2a_task_id` が変わっても同じステップなら同一ノートに収束する。値の単位は上記「戦略境界の保証」節を参照)                                                                                                                                                                                                                                                                                                               |
| `read_note`                   | `strategy_id`, `note_id`                                                                                                                              | ノート本文 / メタデータ / `graphs` (`GraphDef` の配列)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `list_notes`                  | `strategy_id`, `limit?`, `status?` (`approved`/`unread`/`rejected`), `updated_after?`, `include_body?`                                                | ノート一覧 (新しい順)。`status`/`updated_after` で絞り込み可。`include_body: false` を指定すると各ノートの `body_md` が `null` になる (省略時は `true` 相当で常に含む)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| `create_annotation`           | `strategy_id`, `target_symbol`, `target_kind`, `timestamp`, `text`, `price?`, `linked_note_id?`                                                       | アノテーション作成。`target_kind` は自由記述の分類ラベル。resume で同じステップの新しい試行が呼ぶと、前の試行がそのステップで作った未レビュー (`unread`) のアノテーションを削除して置き換える (承認/却下済みのものやコメントが付いたものは残す)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `read_annotations`            | `strategy_id`, `target_symbol?`, `limit?`                                                                                                             | アノテーション一覧 (任意で `target_symbol` によるフィルタが可能)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `read_comments`               | `strategy_id`, `target_kind` (`note` / `annotation`), `target_id`, `resolved?`                                                                        | 対象に付いたレビューコメント一覧 (古い順)。スレッドは `parent_id` で表現。`resolved` で絞り込み可。各コメントは `anchor_text`/`start_line`/`end_line`/`drifted` を含み、ノート本文中の位置を追跡する                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          |
| `resolve_comment`             | `strategy_id`, `comment_id`, `resolved`                                                                                                               | レビューコメントを解決済み/未解決に切り替える                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `reply_comment`               | `strategy_id`, `parent_id`, `body`                                                                                                                    | レビューコメントに返信する。`author_kind` はサーバー側で `"llm"` 固定                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `eval_python`                 | `strategy_id`, `code`, `stdin?`, `timeout_secs?`, `max_output_bytes?`                                                                                 | Python コードを exec Pod (Kata Containers) 上で実行。stdout/stderr/exit code を返す                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `add_interest`                | `strategy_id`, `ref_kind`, `ref_id`                                                                                                                   | 関心 (`role=derived`, `origin=llm`) を追加。既存と一致すれば idempotent に成功                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `list_watch_targets`          | `strategy_id`, `limit?`                                                                                                                               | 人間が `origin=human` で登録した監視対象銘柄 (`ref_kind=stock`, `status=active`) を古い順に返す。口座全体の関心 (`strategy_id IS NULL`) と接続元戦略の関心の両方が対象で、他の戦略にだけ属する関心は含まない。エージェント自身が追加した関心 (`origin=llm`) や `status=archived` のものも含まない。保有状況によるフィルタは行わないため、除外したい場合は `read_portfolio` / `check_buyable_qty` と組み合わせる                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `eval_indicator`              | `strategy_id`, `name`, `args`, `timeout_secs?`, `max_output_bytes?`                                                                                   | 永続化された indicator を exec Pod 上で評価 (戦略 scope 優先、無ければ global)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `search_web`                  | `strategy_id`, `query`                                                                                                                                | 問い合わせ文を web 検索対応モデルに渡し、テキストと出典 URL (`citations`, 重複除去済み) を返す。モデルは `WEB_SEARCH_MODEL` 環境変数で指定する。`add_interest`/RSS で未追跡の銘柄・語・テーマを調べる用途に加え、`read_news`/`search_news` の `body_snippet` (先頭 280 文字止まり) だけでは分からない記事本文を、題名や URL を問い合わせ文にして調べる用途にも使う。戦略タスク実行 (`x-execution-id` の `a2a_task_id` 部分) あたり 20 回まで、超過時は LiteLLM を呼ばずに reject する                                                                                                                                                                                                                                                                                                                                                         |
| `read_portfolio`              | `strategy_id` (戦略スライス算出用、口座全体の集計には使わない)                                                                                        | 口座全体 (全戦略横断) と接続元戦略それぞれの保有銘柄・実現損益 (FIFO) を直近終値で時価評価して返す。戦略側は投資可能額と未使用枠も含む。価格観測日 (`priced_at`) も返す                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `read_trades`                 | `symbol?`, `date_from?` (YYYY-MM-DD), `limit?`                                                                                                        | `read_portfolio` の account スコープと同じ規則で全戦略横断の個々の約定 (約定日・銘柄・売買区分・数量・価格・`strategy_id`) を新しい順に返す。`symbol` の完全一致と `date_from` 以降 (inclusive) で絞り込み可                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| `check_buyable_qty`           | `strategy_id`, `symbol`                                                                                                                               | 指定銘柄をあと何株買えるかを、`account_risk_policy.max_sector_ratio` / 戦略の未使用投資可能額の 2 制約それぞれについて算出し、全体の最小値 (`max_qty`) とどの制約が効いたか (`binding_constraint`) を返す。保有していない銘柄でも `current_qty=0` として計算する。各上限株数は 100 株単位 (`lot_size`) に切り捨てる。制約が未設定なら `status=unlimited`、価格やセクターなど計算に必要な値が欠けていれば `status=unavailable` (`reason` 付き) を返し、誤った数値は返さない                                                                                                                                                                                                                                                                                                                                                                    |
| `read_shareholding_structure` | `strategy_id`, `symbol` (4 桁), `limit?`                                                                                                              | EDINET 取り込みデータ (`edinet_large_volume_shareholdings` / `edinet_major_shareholders` / `edinet_cross_shareholdings`) から銘柄の保有構造を返す。`large_volume_reports` は大量保有報告書と変更報告書を新しい順に (`document_type`、変更報告書のみの `change_reason`/`total_shares_ratio_last`、保有者ごとの `holding_purpose`/`shares_ratio`/`shares_ratio_last` を含む。割合は小数、0.1 = 10%)。`major_shareholders` は直近の大株主状況の上位株主 (順位付き)。`cross_shareholdings` は自社の直近の政策保有株式を保有先ごとに (前期比の株式数と計上額、相手からの保有有無を示す `mutual_holding`)。5 桁の EDINET コードは先頭 4 文字が `symbol` と一致する行を対象にする (対応関係は J-Quants の仕様に明記されていない)。取り込み済みデータが無ければ `major_shareholders`/`cross_shareholdings` は `null`、`large_volume_reports` は空配列 |
| `read_news`                   | `strategy_id`, `limit?`                                                                                                                               | 戦略に紐づいた未読ニュースを古い順に返す (`has_more` で続きの有無を示す)。同一戦略と stream 単位の checkpoint を呼び出しごとに自動で進めるため、実行間隔が空いても取りこぼさない。1 記事が複数の関心に一致していれば一致ごとに 1 行になる (`url` が重複し得る)。`body_snippet` は元フィードの description 先頭 280 文字に切り詰めたもので本文全体ではない。それ以上が必要なら `search_web` で調べる                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| `search_news`                 | `strategy_id`, `keyword?`, `from?` (YYYY-MM-DD), `to?` (YYYY-MM-DD), `limit?`                                                                         | `news_item` を `title`/`body_snippet` の部分一致 (大文字小文字を区別しない) と `published_at` の期間 (共に inclusive) で直接検索し、新しい順に返す。`news_strategy_link` を経由しないため、`strategy_interest` に語が登録されているかどうかに結果は左右されない (`read_news` が引き継いでいた制約を回避する)。`body_snippet` は元フィードの description 先頭 280 文字に切り詰めたもので本文全体ではない。それ以上が必要なら `search_web` で調べる                                                                                                                                                                                                                                                                                                                                                                                             |
| `search_refs`                 | `strategy_id`, `query`, `limit?`                                                                                                                      | 一級参照型 (`stock`/`indicator`/`sector`/`theme`) を `id`/`name`/`ref_term` の別名の部分一致 (大文字小文字・全角半角を区別しない) で横断検索し、`ref_kind`/`ref_id`/`name` を `name` 順で返す。4 テーブルを `UNION ALL` した raw SQL で実装 (umbrella テーブルは作らない)。結果はそのまま `add_interest` の入力に使える                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| `add_ref_terms`               | `strategy_id`, `ref_kind`, `ref_id`, `terms`                                                                                                          | 一級参照型に別名 (表記揺れ・略称・旧社名等) を追加する。同じ (`ref_kind`, `ref_id`, `term`) が既に登録済みなら idempotent にスキップし、返る `added` には含まない。空文字の `term` は無視する                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| `remove_ref_terms`            | `strategy_id`, `ref_kind`, `ref_id`, `terms`                                                                                                          | 一級参照型から別名を削除する。登録されていない `term` を渡しても idempotent にスキップし、返る `removed` には含まない                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `read_fin_summary`            | `strategy_id`, `symbol`, `limit?`                                                                                                                     | 銘柄の財務情報 (J-Quants `/fins/summary`) を新しい順に返す。決算短信の実績値や会社予想に加え、業績予想/配当予想の修正も対象。`symbol` は 4 桁のみ受け付け、J-Quants の 5 桁コードとは先頭 4 文字の一致で突き合わせる。同じ (開示書類種別, 当会計期間) の開示が複数あるとき (訂正等) は開示番号 (`DiscNo`) が最大の 1 件のみ返す。開示元が報告していない項目 (IFRS/米国基準の経常利益等) は null                                                                                                                                                                                                                                                                                                                                                                                                                                               |
| `read_short_sale_reports`     | `strategy_id`, `symbol` (4 桁), `from?`, `to?`, `limit?`                                                                                              | 銘柄の空売り残高報告 (J-Quants `/markets/short-sale-report`) を公表日の新しい順に返す (同一公表日内は報告者名の昇順)。残高割合 0.5% 以上の報告のみ存在するため、結果が空でも「空売りが無い」ではなく「報告義務のある空売りが無い」ことしか意味しない。各行は 1 報告者の 1 件の報告で、`short_position_ratio`/`prev_report_ratio` は小数 (0.01 = 1%) で、両者の差分が前回報告からの増減を表す。初回報告は `prev_report_date`/`prev_report_ratio` が null。`symbol` は先頭 4 文字の一致で J-Quants の 5 桁コードと突き合わせる。`from`/`to` は公表日の範囲 (inclusive、両方省略可)                                                                                                                                                                                                                                                              |
| `read_sector_short_ratio`     | `strategy_id`, `sector`, `from?`, `to?`, `limit?`                                                                                                     | 業種別の空売りの売買代金と空売り比率 (J-Quants `/markets/short-ratio`) を日付の新しい順に返す。`sector` には `sector` テーブル / `search_refs` / `check_buyable_qty` と同じ 33 業種名 (例: "輸送用機器") を指定する。未知の名前は reject する。日ごとに実注文の売買代金 (`sell_excluding_short_value`)、価格規制あり/なしの空売り売買代金 (`short_with_restriction_value`/`short_without_restriction_value`、いずれも円) に加え、算出した `short_ratio` (空売り売買代金 / 売買代金合計、小数、0.1 = 10%) を返す。その業種で売買が無かった日は 4 フィールドとも null。`from`/`to` は対象日の範囲 (inclusive、両方省略可)                                                                                                                                                                                                                       |
| `read_macro_indicator`        | `strategy_id`, `indicator_id`, `from` (YYYY-MM-DD), `to` (YYYY-MM-DD)                                                                                 | マクロ指標 (`indicator_observation`、取り込みは `backend/src/services/fred_ingest.rs`) の日次観測値 (`date`, `value`) を期間内で日付昇順に返す。`indicator_id` は `search_refs` (`ref_kind=indicator`) で発見できる (例: `USDJPY`, `VIX`, `US10Y`, `NIKKEI225`)。値は取り込み元の単位のまま (USDJPY は 1 ドルあたりの円、US10Y は %)。観測が無い日 (休場等) は単に存在しない (補間しない)。USDJPY は取り込み元への反映が週次まとめのため最新観測日が最大 1 週間ほど遅れ得る。該当データが無ければ空配列                                                                                                                                                                                                                                                                                                                                       |
| `read_margin`                 | `strategy_id`, `symbol`, `from?` (YYYY-MM-DD), `to?` (YYYY-MM-DD), `limit?`                                                                           | 銘柄の信用残を返す。`symbol` は 4 桁のみ受け付け、J-Quants の 5 桁コードとは先頭 4 文字の一致で突き合わせる。`interest` は信用取引週末残高 (2026-09-28 以降の切替後は日次の信用取引残高) を新しい順に返し、各行に 5 桁コードと `iss_type` (1: 信用銘柄, 2: 貸借銘柄, 3: その他) を含む。`alerts` は日々公表信用取引残高を新しい順に返し、`pub_reason` (公表理由) と `tse_mrgn_reg_cls` (規制区分) を含む。取引所が日々公表銘柄に指定した銘柄のみが対象で、`alerts` に載っていないことは残高ゼロを意味しない。同一申込日に訂正が複数あれば公表日が最新の 1 件のみ返す。`interest` の金額系フィールド (`shrt_val` 等) は 2026-09-25 申込分より前は null。`limit` は `interest` / `alerts` それぞれに独立に適用する                                                                                                                              |
| `list_hypotheses`             | `strategy_id`, `limit?`                                                                                                                               | 接続元戦略の仮説と account-wide (global, `strategy_id IS NULL`) 仮説を新しい順に返す。他戦略専属の仮説は含まない                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              |
| `read_hypothesis`             | `strategy_id`, `hypothesis_id`                                                                                                                        | 仮説 1 件のタイトル、本文、status を返す。自戦略または global の仮説のみ許可し、他戦略専属の仮説は reject する                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| `propose_hypothesis_change`   | `strategy_id`, `hypothesis_id`, `proposed_title?`, `proposed_body?`, `proposed_status?`, `rationale`                                                  | `proposal_id`, `status` (常に `pending`)。仮説へのタイトル、本文、status の変更提案を永続化するのみで、仮説本体には反映しない。承認/却下は人間が別途 API 経由で行う。対象仮説のスコープは `read_hypothesis` と同じ                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| `record_prediction`           | `strategy_id`, `note_id?`, `target_stock_id`, `benchmark_stock_id`, `direction` (`outperform`/`underperform`), `probability`, `base_date`, `due_date` | 記録した予測 (`PredictionDto`)。`probability` は固定刻み (0.55/0.6/0.65/0.7/0.75/0.8/0.85/0.9) のみ許可し、DB の CHECK 制約で強制する。`base_date` の終値を基準に `due_date` までに `target_stock_id` が `benchmark_stock_id` を上回る/下回るかを表す。write-once であり、更新・削除 tool は存在しない (書き換えると後日のグレーディングが無効になるため)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| `list_predictions`            | `strategy_id`, `limit?`, `due_after?`, `due_before?`                                                                                                  | 接続元戦略が記録した予測一覧 (新しい順)。`due_after`/`due_before` で絞り込み可 (例: `due_after=today` でまだ結果が出ていない予測のみ)                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| `read_prediction_stats`       | `strategy_id`                                                                                                                                         | 接続元戦略の採点済み予測 (`prediction_grade` が付いたもの) を Brier score (確率と的中/非的中の二乗誤差の平均、低いほど較正が良い) と `record_prediction` と同じ確率刻み (0.55/0.6/0.65/0.7/0.75/0.8/0.85/0.9) ごとの件数・的中率 (`hit_rate`、件数 0 の刻みは null) で集計する。未採点の予測は集計から除外するため、`graded_count` は記録済み予測の総数より小さくなり得る                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |

`eval_python` は入力値に対する純粋関数評価モデルとして設計している。1 evaluation = 1 exec Pod で起動し、入出力は stdin → stdout/stderr/exit code のみ。Pod spec 側の隔離 (read-only rootfs、non-root、capabilities drop、deadline 等) は backend が固定する。namespace 側の隔離 (RuntimeClass `kata`、NetworkPolicy 全 deny、Pod Security Admission) は [`docs/deployment.md`](./deployment.md) を参照。

## session 管理方針

`mcp-session-id` ヘッダで識別される session は `LocalSessionManager` の in-memory state のみで管理し、永続化しない。
backend Pod が再起動すると、それまでの `mcp-session-id` は未知の session として扱われる。
クライアントは `initialize` からやり直す。

session は永続化しない。
GET (SSE) 接続のたびに shadow stream (`rmcp` 内部の keep-alive 用チャネル、上限 32) が積まれる一方、クライアントが同じ session id を叩き続ける限り永続化された session はアイドル判定に入らず GC できない。
再起動のたびに同じ session が復元され、shadow stream が上限に達して警告ログが出力され続ける原因になる。

## `MCP_ALLOWED_HOSTS`

`rmcp` の Streamable HTTP transport は DNS rebinding 対策として `Host` ヘッダの allowlist を持つ。既定値は `localhost` / `127.0.0.1` / `::1` のみで、この allowlist に無い `Host` ヘッダで届いたリクエストは全て 403 で弾かれる。判定対象は Ingress/Tunnel がそのまま転送してくる `Host` ヘッダ (または HTTP/2 の `:authority`) の実値で、`X-Forwarded-Host` 等は参照しない。

`MCP_ALLOWED_HOSTS` 環境変数 (カンマ区切り) で allowlist を拡張する。

```
MCP_ALLOWED_HOSTS=<backend-service-fqdn>,<other-host>
```

- port を含めずホスト名だけを書けば任意 port を許容する。
- 空白は trim し、空エントリは無視する。
- in-cluster 配備時は backend Service の DNS 名を必ず追加すること。追加しないと t-rader-agent など in-cluster クライアントからの接続が全て 403 で弾かれる。
- `/mcp/mgmt` を Ingress/Tunnel 経由で外部公開する場合は、その公開ホスト名も追加すること。追加しないと外部の MCP クライアントからの接続が全て 403 で弾かれる (`/mcp/mgmt` と `/mcp/strategy` は allowlist を共有するため、追加した公開ホスト名は両方に適用される)。
- この allowlist は DNS rebinding 対策であり認証機構ではない。外部公開する場合は Ingress/Tunnel 側で bearer token や IdP 連携などの実認証を別途課すこと。パス単位のルーティング制限をしないと、`/mcp/mgmt` 用に追加した公開ホスト名で `eval_python` を含む `/mcp/strategy` にも到達できてしまう。

具体的な配備時の env / RBAC は [`docs/deployment.md`](./deployment.md) を参照。
