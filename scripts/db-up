#!/usr/bin/env bash
#
# DB (docker-compose.infra.yml) を起動する。ランダム割り当てされた実ポートは
# scripts/db-url が都度解決するので、ここではポートの記録は行わない。
#
# Usage:
#   mise run db-up

set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
cd "$SCRIPT_DIR/.."

docker compose -f docker-compose.infra.yml up -d --wait
