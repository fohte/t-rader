#!/usr/bin/env bash
set -euo pipefail

# backend/openapi.json から TypeScript 型定義を生成する。
# Rust ビルドは不要 (openapi.json は git 追跡済み)。

REPO_ROOT="$(cd "$(dirname "$0")/.." && pwd)"

cd "${REPO_ROOT}/frontend" && pnpm run generate:api
cd "${REPO_ROOT}/agent" && pnpm run generate:api
