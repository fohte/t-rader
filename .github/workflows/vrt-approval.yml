name: VRT approval

on:
  pull_request:
    types: [synchronize, labeled]

jobs:
  vrt-approval-frontend:
    permissions:
      contents: read
      pull-requests: write
      statuses: write
    uses: fohte/storybook-vrt-workflows/.github/workflows/vrt-approval.yml@90d01ee2e4d8501cce03b6cb938bb08d5e011edc # v0.1.4
    with:
      r2-bucket: t-rader-vrt
